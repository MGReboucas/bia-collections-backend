# Framework principal
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Validação e configs
pydantic>=2.7.0
pydantic-settings>=2.2.0
email-validator>=2.1.0
jinja2>=3.1.0

# Banco de dados
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9   # PostgreSQL (Render)

# Migrations
alembic>=1.13.0

# Segurança
passlib[bcrypt]>=1.7.4
bcrypt<4.0.0          # passlib 1.7.x is not compatible with bcrypt 4.x
python-jose[cryptography]>=3.3.0
slowapi>=0.1.0        # rate limiting

# Requisições externas (CEP, frete, pagamento)
httpx>=0.27.0
redis>=5.0.0
rq>=1.16.0

# Upload de arquivos
python-multipart>=0.0.9

# Cloud storage (fotos de perfil e produtos)
cloudinary>=1.40.0

# Variáveis de ambiente
python-dotenv>=1.0.0

# Pagamentos
mercadopago>=2.2.0

# Testes
pytest==8.0.2

# (opcional) ambiente
python-dotenv==1.0.1
